# Simple Sign-Up Form

This is a basic HTML form with fields for Name, Email, Password, and Confirm Password.  
It demonstrates HTML form structure and simple CSS styling for layout and design.

**Concepts used:**
- HTML form tags (`<form>`, `<input>`, `<label>`, `<button>`)
- CSS layout (centering, box shadow, border-radius)
